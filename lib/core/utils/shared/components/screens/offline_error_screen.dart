
import 'package:flutter/material.dart';

class OfflineErrorScreen extends StatelessWidget {
  const OfflineErrorScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('HomeStates.connectionError'),
    );
  }
}
